name: projectsol
description: Aplicación de pronóstico solar para ahorro energético.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  geolocator: ^13.0.1
  sensors_plus: ^6.0.1
  fl_chart: ^0.69.0
  flutter_local_notifications: ^17.2.1
  shared_preferences: ^2.3.2
  provider: ^6.1.2
  intl: ^0.19.0
  timezone: ^0.9.4
  flutter_heatmap_calendar: ^1.0.0
  google_maps_flutter: ^2.5.3
  location: ^5.0.3
  geocoding: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  